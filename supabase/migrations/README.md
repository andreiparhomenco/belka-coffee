# 📊 SQL Миграции для Belka Coffee

## Список миграций

| # | Файл | Описание | Статус |
|---|------|----------|--------|
| 001 | `001_create_users.sql` | Таблица пользователей | ✅ Готово |
| 002 | `002_create_shop_template.sql` | Шаблон работы кофейни | ✅ Готово |
| 003 | `003_create_availability.sql` | Доступность бариста | ✅ Готово |
| 004 | `004_create_shifts.sql` | Смены | ✅ Готово |
| 005 | `005_create_shift_reports.sql` | Отчёты о сменах | ✅ Готово |
| 006 | `006_create_salaries.sql` | Зарплаты | ✅ Готово |
| 007 | `007_create_audit_log.sql` | Журнал изменений | ✅ Готово |
| 008 | `008_setup_rls.sql` | Row Level Security | ✅ Готово |

---

## 🗄️ Структура базы данных

```
users (пользователи)
├── id: UUID (PK)
├── telegram_id: BIGINT (UNIQUE)
├── name: TEXT
├── role: 'barista' | 'admin'
└── timestamps

shop_template (шаблон работы)
├── id: UUID (PK)
├── day_of_week: INT (0-6)
├── hour: INT (0-23)
└── is_active: BOOLEAN

availability (доступность)
├── id: UUID (PK)
├── user_id: UUID (FK → users)
├── week_start: DATE
├── day_of_week: INT
└── hour: INT

shifts (смены)
├── id: UUID (PK)
├── user_id: UUID (FK → users)
├── week_start: DATE
├── day_of_week: INT
├── hour: INT
├── status: TEXT
└── timestamps

shift_reports (отчёты)
├── id: UUID (PK)
├── shift_id: UUID (FK → shifts)
├── turnover: NUMERIC
├── confirmed_hours: NUMERIC
└── submitted_at: TIMESTAMPTZ

salaries (зарплаты)
├── id: UUID (PK)
├── user_id: UUID (FK → users)
├── week_start: DATE
├── total_hours: NUMERIC
├── total_turnover: NUMERIC
├── salary: NUMERIC
└── is_paid: BOOLEAN

audit_log (аудит)
├── id: UUID (PK)
├── user_id: UUID (FK → users)
├── action: TEXT
├── table_name: TEXT
├── old_data: JSONB
├── new_data: JSONB
└── created_at: TIMESTAMPTZ
```

---

## 🚀 Применение миграций

### Вариант 1: Через Supabase Dashboard

1. Откройте: https://supabase.com/dashboard/project/jcrjcglfzrhcghiqfltp
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое каждого файла миграции
4. Выполните по порядку (001 → 002 → ... → 008)

### Вариант 2: Через Supabase CLI

```bash
# Установить CLI (если ещё не установлен)
npm install -g supabase

# Логин
supabase login

# Связать проект
supabase link --project-ref jcrjcglfzrhcghiqfltp

# Применить все миграции
supabase db push

# Или применить конкретную миграцию
supabase db push --include-migrations 001
```

---

## 🔐 Row Level Security (RLS)

### Политики для бариста:
- ✅ Видят только свои данные (availability, shifts, salaries)
- ✅ Могут создавать свою доступность
- ✅ Могут подтверждать свои смены
- ✅ Могут создавать отчёты о сменах
- ❌ Не могут видеть данные других бариста

### Политики для админов:
- ✅ Видят все данные
- ✅ Могут управлять всеми таблицами
- ✅ Могут редактировать прошлые смены
- ✅ Видят audit log

---

## 📈 Полезные SQL функции

### 1. Расчёт зарплаты

```sql
-- Для конкретного бариста
SELECT * FROM calculate_salary(
  'user-id-here'::UUID, 
  '2025-01-13'::DATE
);

-- Для всех бариста
SELECT * FROM calculate_all_salaries('2025-01-13'::DATE);
```

### 2. Статистика отчётов

```sql
SELECT * FROM get_week_reports_stats('2025-01-13'::DATE);
```

### 3. Активные смены

```sql
SELECT * FROM get_active_shifts('2025-01-13'::DATE);
```

### 4. История изменений

```sql
SELECT * FROM get_record_history(
  'shifts', 
  'shift-id-here'::UUID
);
```

---

## 🧹 Очистка старых данных

```sql
-- Очистить старую доступность (>4 недель)
SELECT cleanup_old_availability();

-- Очистить старые логи (>6 месяцев)
SELECT cleanup_old_audit_logs();
```

---

## 🧪 Тестовые данные

Для тестирования можно добавить:

```sql
-- Создать админа
INSERT INTO users (telegram_id, name, role)
VALUES (123456789, 'Админ Тестовый', 'admin');

-- Создать бариста
INSERT INTO users (telegram_id, name, role)
VALUES 
  (111222333, 'Иван Петров', 'barista'),
  (444555666, 'Мария Сидорова', 'barista');
```

---

## ⚠️ Важные замечания

1. **Порядок миграций важен!** Применяйте строго по номерам
2. **RLS включен!** Тестируйте с правильными правами доступа
3. **Audit log работает!** Все изменения логируются
4. **Триггеры активны!** updated_at обновляется автоматически
5. **Уникальные ключи!** Нельзя создать дубликаты смен на один слот

---

## 📞 Troubleshooting

### Ошибка: "relation already exists"
Таблица уже создана. Либо пропустите, либо удалите через:
```sql
DROP TABLE IF EXISTS table_name CASCADE;
```

### Ошибка: "permission denied"
Убедитесь что используете **service_role** key в CLI или админский аккаунт в Dashboard.

### Ошибка при RLS: "no rows returned"
Проверьте что `auth.uid()` установлен правильно. В тестах используйте service_role key.

---

**Создано:** 15 декабря 2025  
**Версия:** 1.0  
**Проект:** Belka Coffee

