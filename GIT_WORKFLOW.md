# 📦 Git Workflow для проекта Belka Coffee

## ✅ Репозиторий готов!

**URL:** https://github.com/andreiparhomenco/belka-coffee

## 📊 Что загружено

```
✅ 33 файла
✅ 9,375 строк кода
✅ Первый коммит: "🎉 Этап 0: Подготовка окружения завершён"
```

---

## 🔄 Базовый Git Workflow

### 1. Проверка статуса

```bash
git status
```

### 2. Добавление изменений

```bash
# Добавить все изменения
git add .

# Или добавить конкретные файлы
git add frontend/src/components/MyComponent.tsx
```

### 3. Создание коммита

```bash
# Короткое сообщение
git commit -m "feat: добавлен компонент выбора доступности"

# Или с подробным описанием
git commit -m "feat: добавлен компонент AvailabilitySelector

- Создан компонент для выбора часов
- Добавлены тесты
- Обновлена документация"
```

### 4. Отправка в GitHub

```bash
# Отправить в main
git push origin main

# Или просто
git push
```

---

## 🌿 Работа с ветками

### Создание новой ветки

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/availability-selector

# Или через новый синтаксис
git switch -c feature/availability-selector
```

### Переключение между ветками

```bash
# Переключиться на main
git checkout main

# Или
git switch main
```

### Слияние веток

```bash
# Находясь на main, слить feature ветку
git checkout main
git merge feature/availability-selector
```

### Удаление ветки

```bash
# Локально
git branch -d feature/availability-selector

# Удалённо
git push origin --delete feature/availability-selector
```

---

## 📝 Соглашения о коммитах (Conventional Commits)

Рекомендуется использовать следующий формат:

```
<type>: <subject>

<body>
```

### Типы коммитов:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, пропущенные точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигов и т.д.

### Примеры:

```bash
git commit -m "feat: добавлен компонент AvailabilitySelector"
git commit -m "fix: исправлена ошибка валидации часов"
git commit -m "docs: обновлён README с инструкциями"
git commit -m "test: добавлены unit тесты для helpers"
git commit -m "chore: обновлены зависимости"
```

---

## 🔙 Откат изменений

### Отменить последний коммит (но оставить изменения)

```bash
git reset --soft HEAD~1
```

### Отменить последний коммит (и удалить изменения)

```bash
git reset --hard HEAD~1
```

### Отменить изменения в файле

```bash
git checkout -- filename.ts
```

---

## 📥 Получение изменений

### Получить изменения с GitHub

```bash
git pull origin main
```

### Получить информацию о удалённом репозитории

```bash
git remote -v
```

---

## 🏷️ Создание релизов (тегов)

### Создание тега

```bash
# Лёгкий тег
git tag v0.1.0

# Аннотированный тег (рекомендуется)
git tag -a v0.1.0 -m "Этап 0: Подготовка окружения"
```

### Отправка тегов

```bash
# Отправить конкретный тег
git push origin v0.1.0

# Отправить все теги
git push --tags
```

### Просмотр тегов

```bash
git tag
```

---

## 🔍 Просмотр истории

### Просмотр логов

```bash
# Полный лог
git log

# Короткий лог
git log --oneline

# График веток
git log --oneline --graph --all

# Последние 5 коммитов
git log -5
```

### Просмотр изменений в коммите

```bash
git show <commit-hash>
```

---

## 🚀 Рекомендуемый workflow для проекта

### Для новой функции:

1. **Создать ветку от main:**
   ```bash
   git checkout main
   git pull
   git checkout -b feature/название-функции
   ```

2. **Разработка:**
   - Пишете код
   - Тестируете
   - Коммитите регулярно
   ```bash
   git add .
   git commit -m "feat: описание изменений"
   ```

3. **Отправить в GitHub:**
   ```bash
   git push origin feature/название-функции
   ```

4. **Создать Pull Request на GitHub**

5. **После ревью - слить в main:**
   ```bash
   git checkout main
   git pull
   git merge feature/название-функции
   git push
   ```

### Для быстрых фиксов:

```bash
git checkout main
git pull
# Внести изменения
git add .
git commit -m "fix: описание фикса"
git push
```

---

## 📌 Полезные команды

### Посмотреть изменения до коммита

```bash
git diff
```

### Посмотреть изменения в staged файлах

```bash
git diff --staged
```

### Временно сохранить изменения

```bash
# Сохранить
git stash

# Посмотреть список
git stash list

# Применить последние изменения
git stash pop

# Применить конкретный stash
git stash apply stash@{0}
```

### Удалить неотслеживаемые файлы

```bash
# Посмотреть что будет удалено
git clean -n

# Удалить файлы
git clean -f

# Удалить файлы и папки
git clean -fd
```

---

## 🔐 Безопасность

### ⚠️ НИКОГДА не коммитьте:

- ❌ Файлы `.env` с секретами
- ❌ API ключи и токены
- ❌ Пароли к базам данных
- ❌ Private keys
- ❌ Папку `node_modules/`

### ✅ Используйте `.gitignore`

Файл `.gitignore` уже настроен в проекте и игнорирует:
- `.env` файлы
- `node_modules/`
- `dist/`
- IDE файлы

---

## 📞 Помощь

### Если что-то пошло не так:

```bash
# Посмотреть статус
git status

# Посмотреть что изменилось
git log --oneline -5

# Откатиться к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit-hash>
```

### Если нужно откатить push:

```bash
# ⚠️ ОСТОРОЖНО! Это перезапишет историю
git push --force origin main
```

---

## 🎯 Рекомендации для команды

1. **Делайте коммиты часто** - маленькие атомарные коммиты лучше больших
2. **Пишите понятные сообщения** - чтобы через месяц понять что вы делали
3. **Используйте ветки** - для каждой новой функции
4. **Делайте Pull Request** - для code review (если работаете в команде)
5. **Синхронизируйтесь часто** - делайте `git pull` перед началом работы

---

**Репозиторий:** https://github.com/andreiparhomenco/belka-coffee  
**Документация:** см. README.md и docs/

